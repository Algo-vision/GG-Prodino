.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
.venv/
*.pyc
__pycache__/
.pytest_cache/
.ipynb_checkpoints/
build/
GG-GRK-GUI-*.spec
server/web_ui/node_modules
server/web_ui/certs
keys/
server/web_ui/.env
server/web_ui/data/grk_users.db
code_example_versions
.gitignore
HLC1-IMU_Directions.jpg
MSB IMUs Oriantations (U8-1,U7-2,U7-Combined).jpeg
new_task_12_07.txt
GRK_Board_Direct_keys/
# AWS IoT device credentials embedded for the firmware (contains a PRIVATE KEY - never commit)
firmware/src/aws_client_certs.h
GRK_Board_Direct_keys/
firmware/src/aws_certs_store.h

# TEMPORARY - Option-A (on-board TLS) leftovers, not used by the current Option-B
# firmware (SSLClient is lib_ignore'd; aws_root_ca.h isn't included anywhere).
# Ignored for now to keep them out of git. TODO: if/when on-board TLS is revisited
# (needs more-RAM hardware), REMOVE these two lines and commit both.
firmware/lib/SSLClient/
firmware/src/aws_root_ca.h
