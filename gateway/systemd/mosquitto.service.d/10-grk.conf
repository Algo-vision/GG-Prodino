[Unit]
# Make Mosquitto wait for the board cert/serial fetch before it starts, so the
# TLS bridge comes up with fresh certs. (grk-fetch-certs also has Before=mosquitto,
# so ordering holds even without this drop-in; this makes the intent explicit.)
After=grk-fetch-certs.service
Wants=grk-fetch-certs.service
